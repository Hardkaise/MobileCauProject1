<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".view.homepage.MemoListFragment"
    android:background="@color/white">

    <TextView
        android:id="@+id/memo.list.title"
        android:layout_alignParentStart="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_button"
        style="@style/alarm"
        android:padding="10dp"
        android:layout_marginTop="40dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:text="@string/memo_list" />

    <TextView
        android:id="@+id/memo.list.no_memo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:textColor="@color/black"
        android:textSize="20sp"
        android:layout_centerInParent="true"
        android:text="@string/no_memo_please_add_in_calendar" />

    <android.support.v7.widget.RecyclerView
        android:layout_below="@+id/memo.list.title"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/memo.list_view"
        android:layout_marginTop="15dp"/>
</RelativeLayout>